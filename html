<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Rubex UI — Building Digital Experiences That Drive Growth</title>
  <meta name="description" content="Custom websites, web applications, and digital solutions engineered for performance and scalability. Rubex UI is your forward-thinking web development partner." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Syne:wght@700;800&display=swap" rel="stylesheet" />
  <style>
    /* ─── Reset & Base ─────────────────────────────────────── */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --blue:    #2563EB;
      --blue-lt: #3B82F6;
      --slate:   #0F172A;
      --cyan:    #06B6D4;
      --cyan-lt: #22D3EE;
      --white:   #FFFFFF;
      --gray-50: #F8FAFC;
      --gray-100:#F1F5F9;
      --gray-200:#E2E8F0;
      --gray-400:#94A3B8;
      --gray-600:#475569;
      --gray-800:#1E293B;
      --font-display: 'Syne', sans-serif;
      --font-body:    'Inter', sans-serif;
      --nav-h: 72px;
    }
    html { font-family: var(--font-body); color: var(--slate); background: var(--white); }
    body { overflow-x: hidden; }
    img { max-width: 100%; display: block; }
    a { text-decoration: none; color: inherit; }

    /* ─── Utilities ─────────────────────────────────────────── */
    .container { max-width: 1200px; margin-inline: auto; padding-inline: 24px; }
    .section { padding-block: 100px; }
    .section--alt { background: var(--gray-50); }
    .eyebrow {
      display: inline-flex; align-items: center; gap: 8px;
      font-size: 12px; font-weight: 600; letter-spacing: .12em;
      text-transform: uppercase; color: var(--cyan);
      margin-bottom: 16px;
    }
    .eyebrow::before {
      content: ''; display: block; width: 24px; height: 2px;
      background: var(--cyan);
    }
    .section-title {
      font-family: var(--font-display);
      font-size: clamp(2rem, 4vw, 3rem);
      font-weight: 800; line-height: 1.15;
      color: var(--slate); margin-bottom: 20px;
    }
    .section-sub {
      font-size: 1.125rem; color: var(--gray-600);
      max-width: 560px; line-height: 1.7;
    }
    .btn {
      display: inline-flex; align-items: center; gap: 8px;
      padding: 14px 28px; border-radius: 8px; font-weight: 600;
      font-size: 15px; cursor: pointer; border: none;
      transition: transform .2s, box-shadow .2s, background .2s;
    }
    .btn:hover { transform: translateY(-2px); }
    .btn-primary {
      background: var(--blue); color: var(--white);
      box-shadow: 0 4px 24px rgba(37,99,235,.35);
    }
    .btn-primary:hover { background: var(--blue-lt); box-shadow: 0 8px 32px rgba(37,99,235,.45); }
    .btn-outline {
      background: transparent; color: var(--white);
      border: 1.5px solid rgba(255,255,255,.35);
    }
    .btn-outline:hover { background: rgba(255,255,255,.08); border-color: rgba(255,255,255,.6); }
    .badge {
      display: inline-block; padding: 4px 12px; border-radius: 20px;
      font-size: 12px; font-weight: 600; letter-spacing: .05em;
    }

    /* ─── NAV ───────────────────────────────────────────────── */
    #nav {
      position: fixed; top: 0; inset-inline: 0; z-index: 100;
      height: var(--nav-h);
      transition: background .3s, backdrop-filter .3s, box-shadow .3s;
    }
    #nav.scrolled {
      background: rgba(15,23,42,.88);
      backdrop-filter: blur(16px);
      box-shadow: 0 1px 0 rgba(255,255,255,.06);
    }
    .nav-inner {
      height: 100%; display: flex; align-items: center;
      justify-content: space-between;
    }
    .nav-logo {
      font-family: var(--font-display); font-size: 22px; font-weight: 800;
      color: var(--white); letter-spacing: -.02em;
    }
    .nav-logo span { color: var(--cyan); }
    .nav-links {
      display: flex; align-items: center; gap: 36px;
      list-style: none;
    }
    .nav-links a {
      font-size: 14px; font-weight: 500; color: rgba(255,255,255,.75);
      transition: color .2s;
    }
    .nav-links a:hover { color: var(--white); }
    .nav-cta { display: flex; gap: 12px; align-items: center; }
    .hamburger {
      display: none; flex-direction: column; gap: 5px; cursor: pointer;
      background: none; border: none; padding: 4px;
    }
    .hamburger span {
      display: block; width: 24px; height: 2px; background: var(--white);
      transition: transform .3s, opacity .3s;
    }
    .mobile-menu {
      display: none; position: fixed; inset: 0; z-index: 99;
      background: var(--slate); padding: 90px 24px 40px;
      flex-direction: column; gap: 32px;
    }
    .mobile-menu.open { display: flex; }
    .mobile-menu a { font-size: 22px; font-weight: 600; color: var(--white); }

    /* ─── HERO ──────────────────────────────────────────────── */
    #hero {
      min-height: 100vh; display: flex; align-items: center;
      background: var(--slate); position: relative; overflow: hidden;
      padding-top: var(--nav-h);
    }
    .hero-bg {
      position: absolute; inset: 0; z-index: 0;
      background:
        radial-gradient(ellipse 80% 60% at 70% 50%, rgba(6,182,212,.12) 0%, transparent 70%),
        radial-gradient(ellipse 50% 50% at 10% 80%, rgba(37,99,235,.18) 0%, transparent 60%);
    }
    /* grid lines */
    .hero-grid {
      position: absolute; inset: 0; z-index: 0;
      background-image:
        linear-gradient(rgba(255,255,255,.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,.03) 1px, transparent 1px);
      background-size: 60px 60px;
    }
    /* floating orbs */
    .orb {
      position: absolute; border-radius: 50%;
      filter: blur(80px); opacity: .35;
    }
    .orb-1 { width: 420px; height: 420px; background: var(--blue); top: -80px; right: 10%; animation: drift 8s ease-in-out infinite; }
    .orb-2 { width: 260px; height: 260px; background: var(--cyan); bottom: 10%; left: 5%; animation: drift 11s ease-in-out infinite reverse; }
    .orb-3 { width: 180px; height: 180px; background: var(--blue-lt); top: 40%; right: 30%; animation: drift 14s ease-in-out infinite 2s; }
    @keyframes drift {
      0%,100% { transform: translateY(0) translateX(0); }
      33% { transform: translateY(-30px) translateX(20px); }
      66% { transform: translateY(20px) translateX(-15px); }
    }
    .hero-content {
      position: relative; z-index: 1;
      max-width: 800px;
    }
    .hero-pill {
      display: inline-flex; align-items: center; gap: 8px;
      background: rgba(6,182,212,.12); border: 1px solid rgba(6,182,212,.3);
      color: var(--cyan); font-size: 13px; font-weight: 500;
      padding: 6px 16px; border-radius: 100px; margin-bottom: 32px;
    }
    .hero-pill-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--cyan); animation: pulse 2s infinite; }
    @keyframes pulse { 0%,100% { opacity:1; } 50% { opacity:.3; } }
    .hero-h1 {
      font-family: var(--font-display);
      font-size: clamp(2.5rem, 6vw, 4.5rem);
      font-weight: 800; line-height: 1.08; letter-spacing: -.03em;
      color: var(--white); margin-bottom: 24px;
    }
    .hero-h1 .accent { color: var(--cyan); }
    .hero-sub {
      font-size: clamp(1rem, 2vw, 1.2rem); color: rgba(255,255,255,.65);
      max-width: 580px; line-height: 1.75; margin-bottom: 40px;
    }
    .hero-btns { display: flex; gap: 14px; flex-wrap: wrap; }
    .hero-stats {
      position: relative; z-index: 1;
      margin-top: 72px; display: flex; gap: 40px; flex-wrap: wrap;
      border-top: 1px solid rgba(255,255,255,.08); padding-top: 40px;
    }
    .hero-stat-num {
      font-family: var(--font-display); font-size: 2rem; font-weight: 800;
      color: var(--white); line-height: 1;
    }
    .hero-stat-num span { color: var(--cyan); }
    .hero-stat-label { font-size: 13px; color: rgba(255,255,255,.5); margin-top: 4px; }

    /* floating UI card in hero */
    .hero-card {
      position: absolute; z-index: 2;
      background: rgba(255,255,255,.06);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255,255,255,.12);
      border-radius: 16px; padding: 20px 24px;
      animation: float 6s ease-in-out infinite;
    }
    @keyframes float {
      0%,100% { transform: translateY(0); }
      50% { transform: translateY(-12px); }
    }
    .hero-card-1 {
      right: 4%; top: 28%;
      min-width: 200px;
    }
    .hero-card-2 {
      right: 18%; bottom: 22%;
      min-width: 180px;
      animation-delay: 2s;
    }
    .card-label { font-size: 11px; color: rgba(255,255,255,.45); font-weight: 500; letter-spacing: .06em; text-transform: uppercase; margin-bottom: 10px; }
    .card-metric { font-family: var(--font-display); font-size: 1.6rem; font-weight: 800; color: var(--white); }
    .card-sub { font-size: 12px; color: rgba(255,255,255,.5); margin-top: 3px; }
    .card-bar { margin-top: 12px; height: 4px; background: rgba(255,255,255,.1); border-radius: 2px; overflow: hidden; }
    .card-bar-fill { height: 100%; border-radius: 2px; background: linear-gradient(90deg, var(--blue), var(--cyan)); }
    .card-stack { display: flex; align-items: center; gap: 4px; margin-top: 10px; }
    .card-avatar {
      width: 28px; height: 28px; border-radius: 50%; border: 2px solid var(--slate);
      display: flex; align-items: center; justify-content: center;
      font-size: 11px; font-weight: 700; color: var(--white);
    }
    .card-avatar:nth-child(1) { background: #7C3AED; margin-left: 0; }
    .card-avatar:nth-child(2) { background: var(--blue); margin-left: -8px; }
    .card-avatar:nth-child(3) { background: var(--cyan); margin-left: -8px; }
    .card-avatar:nth-child(4) { background: #10B981; margin-left: -8px; }
    .card-count { font-size: 12px; color: rgba(255,255,255,.6); margin-left: 8px; }

    /* ─── ABOUT ─────────────────────────────────────────────── */
    .about-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 72px; align-items: center; }
    .about-img-wrap { position: relative; }
    .about-img-bg {
      width: 100%; aspect-ratio: 4/3; border-radius: 20px;
      background: linear-gradient(135deg, var(--blue) 0%, var(--cyan) 100%);
      position: relative; overflow: hidden;
    }
    .about-img-bg::after {
      content: ''; position: absolute; inset: 0;
      background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Ccircle cx='30' cy='30' r='2'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    }
    .about-inner-card {
      position: absolute; bottom: -24px; right: -24px;
      background: var(--white); border-radius: 14px;
      padding: 20px 24px; box-shadow: 0 20px 60px rgba(15,23,42,.15);
      min-width: 170px;
    }
    .about-inner-card-num { font-family: var(--font-display); font-size: 2rem; font-weight: 800; color: var(--blue); }
    .about-inner-card-label { font-size: 13px; color: var(--gray-600); margin-top: 2px; }
    .about-badge-wrap {
      position: absolute; top: -16px; left: -16px;
      background: var(--slate); border-radius: 12px;
      padding: 12px 18px; box-shadow: 0 8px 32px rgba(15,23,42,.3);
      display: flex; align-items: center; gap: 10px;
    }
    .about-badge-icon { font-size: 20px; }
    .about-badge-text { font-size: 13px; font-weight: 600; color: var(--white); }
    .about-stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; margin-top: 40px; }
    .about-stat {
      background: var(--gray-50); border-radius: 12px; padding: 20px;
      border: 1px solid var(--gray-200); text-align: center;
    }
    .about-stat-num { font-family: var(--font-display); font-size: 1.75rem; font-weight: 800; color: var(--blue); }
    .about-stat-label { font-size: 12px; color: var(--gray-600); margin-top: 4px; font-weight: 500; }

    /* ─── SERVICES ──────────────────────────────────────────── */
    .services-header { text-align: center; max-width: 640px; margin-inline: auto; margin-bottom: 60px; }
    .services-header .section-sub { margin-inline: auto; }
    .services-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 24px; }
    .service-card {
      background: var(--white); border: 1px solid var(--gray-200);
      border-radius: 16px; padding: 32px;
      transition: transform .25s, box-shadow .25s, border-color .25s;
      cursor: default; position: relative; overflow: hidden;
    }
    .service-card::before {
      content: ''; position: absolute; inset: 0;
      background: linear-gradient(135deg, rgba(37,99,235,.04), rgba(6,182,212,.04));
      opacity: 0; transition: opacity .3s;
    }
    .service-card:hover { transform: translateY(-6px); box-shadow: 0 20px 60px rgba(37,99,235,.12); border-color: rgba(37,99,235,.2); }
    .service-card:hover::before { opacity: 1; }
    .service-icon {
      width: 52px; height: 52px; border-radius: 12px;
      background: linear-gradient(135deg, rgba(37,99,235,.1), rgba(6,182,212,.1));
      display: flex; align-items: center; justify-content: center;
      font-size: 22px; margin-bottom: 20px;
    }
    .service-title { font-weight: 700; font-size: 1.05rem; color: var(--slate); margin-bottom: 10px; }
    .service-desc { font-size: 14px; color: var(--gray-600); line-height: 1.65; }
    .service-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 16px; }
    .service-tag {
      font-size: 11px; font-weight: 600; padding: 3px 10px;
      border-radius: 20px; background: var(--gray-100); color: var(--gray-600);
    }

    /* ─── PORTFOLIO ─────────────────────────────────────────── */
    #portfolio { background: var(--slate); }
    #portfolio .section-title { color: var(--white); }
    #portfolio .section-sub { color: rgba(255,255,255,.55); }
    .portfolio-header { display: flex; align-items: flex-end; justify-content: space-between; flex-wrap: wrap; gap: 20px; margin-bottom: 52px; }
    .portfolio-grid { display: grid; grid-template-columns: repeat(12, 1fr); gap: 20px; }
    .proj-card {
      border-radius: 16px; overflow: hidden; position: relative;
      cursor: pointer; group: true;
    }
    .proj-card:nth-child(1) { grid-column: span 7; }
    .proj-card:nth-child(2) { grid-column: span 5; }
    .proj-card:nth-child(3) { grid-column: span 5; }
    .proj-card:nth-child(4) { grid-column: span 7; }
    .proj-img {
      width: 100%; aspect-ratio: 16/10; object-fit: cover;
      display: block; transition: transform .5s;
    }
    .proj-card:hover .proj-img { transform: scale(1.05); }
    /* colored placeholder images */
    .proj-mock {
      width: 100%; aspect-ratio: 16/10;
      display: flex; flex-direction: column;
      transition: transform .5s;
    }
    .proj-card:hover .proj-mock { transform: scale(1.05); }
    .proj-overlay {
      position: absolute; inset: 0;
      background: linear-gradient(to top, rgba(15,23,42,.95) 0%, transparent 55%);
      padding: 24px; display: flex; flex-direction: column; justify-content: flex-end;
      opacity: 0; transition: opacity .3s;
    }
    .proj-card:hover .proj-overlay { opacity: 1; }
    .proj-overlay-always {
      position: absolute; inset: 0;
      background: linear-gradient(to top, rgba(15,23,42,.85) 0%, transparent 45%);
      padding: 24px; display: flex; flex-direction: column; justify-content: flex-end;
    }
    .proj-category { font-size: 11px; font-weight: 600; letter-spacing: .1em; text-transform: uppercase; color: var(--cyan); margin-bottom: 6px; }
    .proj-name { font-family: var(--font-display); font-size: 1.15rem; font-weight: 700; color: var(--white); margin-bottom: 8px; }
    .proj-tech { display: flex; gap: 6px; flex-wrap: wrap; }
    .proj-tech span {
      font-size: 11px; padding: 2px 8px; border-radius: 4px;
      background: rgba(255,255,255,.12); color: rgba(255,255,255,.8); font-weight: 500;
    }
    .proj-result { font-size: 13px; color: var(--cyan); font-weight: 600; margin-top: 6px; }

    /* project bg visuals */
    .pm1 { background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%); }
    .pm2 { background: linear-gradient(135deg, #0d1b2a 0%, #1b263b 60%, #415a77 100%); }
    .pm3 { background: linear-gradient(135deg, #10002b 0%, #240046 60%, #3c096c 100%); }
    .pm4 { background: linear-gradient(135deg, #012a4a 0%, #013a63 60%, #01497c 100%); }
    .pm-content { padding: 20px; flex: 1; display: flex; flex-direction: column; gap: 8px; }
    .pm-bar { height: 6px; border-radius: 3px; background: rgba(255,255,255,.1); }
    .pm-bar-fill { height: 100%; border-radius: 3px; }
    .pm-row { display: flex; gap: 8px; }
    .pm-block { border-radius: 8px; background: rgba(255,255,255,.06); }
    .pm-nav { display: flex; align-items: center; gap: 8px; padding: 10px 16px; border-bottom: 1px solid rgba(255,255,255,.06); }
    .pm-dot { width: 8px; height: 8px; border-radius: 50%; }
    .pm-logo { font-size: 11px; font-weight: 700; color: rgba(255,255,255,.7); margin-left: 6px; }
    .pm-links { display: flex; gap: 12px; margin-left: auto; }
    .pm-link { width: 32px; height: 6px; border-radius: 3px; background: rgba(255,255,255,.12); }
    .pm-hero-text { padding: 16px 16px 0; }
    .pm-headline { height: 12px; border-radius: 3px; background: rgba(255,255,255,.25); margin-bottom: 8px; }
    .pm-subline { height: 8px; border-radius: 3px; background: rgba(255,255,255,.12); width: 70%; }
    .pm-btn { height: 24px; width: 80px; border-radius: 5px; margin-top: 12px; }

    /* ─── PROCESS ───────────────────────────────────────────── */
    .process-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 2px; margin-top: 60px; position: relative; }
    .process-grid::before {
      content: ''; position: absolute;
      top: 40px; left: calc(12.5% + 24px); right: calc(12.5% + 24px);
      height: 2px;
      background: linear-gradient(90deg, var(--blue), var(--cyan));
      z-index: 0;
    }
    .process-step { padding: 0 20px; text-align: center; position: relative; z-index: 1; }
    .process-num {
      width: 80px; height: 80px; border-radius: 50%; margin-inline: auto; margin-bottom: 24px;
      display: flex; align-items: center; justify-content: center;
      font-family: var(--font-display); font-size: 1.4rem; font-weight: 800;
      position: relative;
    }
    .process-num-inner {
      width: 80px; height: 80px; border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      background: var(--white); border: 2px solid var(--gray-200);
      font-family: var(--font-display); font-size: 1.3rem; font-weight: 800;
      color: var(--blue);
      transition: background .3s, border-color .3s;
    }
    .process-step:hover .process-num-inner { background: var(--blue); color: var(--white); border-color: var(--blue); }
    .process-icon { font-size: 28px; margin-bottom: 12px; display: block; }
    .process-step-title { font-weight: 700; font-size: 1rem; color: var(--slate); margin-bottom: 10px; }
    .process-step-desc { font-size: 14px; color: var(--gray-600); line-height: 1.6; }

    /* ─── TESTIMONIALS ──────────────────────────────────────── */
    .testi-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; margin-top: 52px; }
    .testi-card {
      background: var(--white); border: 1px solid var(--gray-200);
      border-radius: 16px; padding: 32px;
      box-shadow: 0 4px 20px rgba(15,23,42,.05);
      transition: transform .25s, box-shadow .25s;
    }
    .testi-card:hover { transform: translateY(-4px); box-shadow: 0 12px 40px rgba(15,23,42,.1); }
    .testi-stars { display: flex; gap: 3px; margin-bottom: 16px; }
    .testi-star { color: #F59E0B; font-size: 16px; }
    .testi-text { font-size: 15px; color: var(--gray-600); line-height: 1.7; margin-bottom: 24px; font-style: italic; }
    .testi-author { display: flex; align-items: center; gap: 12px; }
    .testi-avatar {
      width: 44px; height: 44px; border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      font-weight: 700; font-size: 16px; color: var(--white); flex-shrink: 0;
    }
    .testi-name { font-weight: 700; font-size: 15px; color: var(--slate); }
    .testi-role { font-size: 13px; color: var(--gray-400); }

    /* ─── WHY CHOOSE ────────────────────────────────────────── */
    .why-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 28px; margin-top: 56px; }
    .why-card {
      display: flex; gap: 16px; padding: 28px; border-radius: 16px;
      background: var(--white); border: 1px solid var(--gray-200);
      transition: transform .2s, box-shadow .2s;
    }
    .why-card:hover { transform: translateY(-3px); box-shadow: 0 10px 30px rgba(37,99,235,.08); }
    .why-icon {
      width: 44px; height: 44px; flex-shrink: 0; border-radius: 10px;
      background: linear-gradient(135deg, rgba(37,99,235,.1), rgba(6,182,212,.1));
      display: flex; align-items: center; justify-content: center; font-size: 20px;
    }
    .why-title { font-weight: 700; font-size: 15px; color: var(--slate); margin-bottom: 6px; }
    .why-desc { font-size: 13px; color: var(--gray-600); line-height: 1.6; }

    /* ─── TECHNOLOGIES ──────────────────────────────────────── */
    #technologies { background: var(--slate); }
    .tech-header { text-align: center; margin-bottom: 52px; }
    #technologies .section-title { color: var(--white); }
    #technologies .section-sub { color: rgba(255,255,255,.55); margin-inline: auto; }
    .tech-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; }
    .tech-card {
      background: rgba(255,255,255,.04); border: 1px solid rgba(255,255,255,.08);
      border-radius: 14px; padding: 28px 20px;
      text-align: center; transition: background .25s, border-color .25s, transform .25s;
      cursor: default;
    }
    .tech-card:hover { background: rgba(37,99,235,.12); border-color: rgba(37,99,235,.3); transform: translateY(-4px); }
    .tech-logo { font-size: 40px; margin-bottom: 12px; display: block; }
    .tech-name { font-weight: 600; font-size: 14px; color: rgba(255,255,255,.85); }
    .tech-desc { font-size: 12px; color: rgba(255,255,255,.4); margin-top: 4px; }

    /* ─── CONTACT ───────────────────────────────────────────── */
    .contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: start; }
    .contact-info { }
    .contact-item { display: flex; align-items: flex-start; gap: 16px; margin-bottom: 28px; }
    .contact-item-icon {
      width: 44px; height: 44px; border-radius: 10px; flex-shrink: 0;
      background: linear-gradient(135deg, rgba(37,99,235,.1), rgba(6,182,212,.1));
      display: flex; align-items: center; justify-content: center; font-size: 18px;
    }
    .contact-item-label { font-size: 12px; font-weight: 600; color: var(--gray-400); text-transform: uppercase; letter-spacing: .06em; }
    .contact-item-value { font-size: 15px; font-weight: 600; color: var(--slate); margin-top: 2px; }
    .social-links { display: flex; gap: 12px; margin-top: 32px; }
    .social-link {
      width: 42px; height: 42px; border-radius: 10px; border: 1.5px solid var(--gray-200);
      display: flex; align-items: center; justify-content: center; font-size: 18px;
      transition: background .2s, border-color .2s, transform .2s; cursor: pointer;
    }
    .social-link:hover { background: var(--blue); border-color: var(--blue); transform: translateY(-2px); }
    .contact-form { background: var(--white); border: 1px solid var(--gray-200); border-radius: 20px; padding: 40px; box-shadow: 0 4px 24px rgba(15,23,42,.06); }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
    .form-group { margin-bottom: 20px; }
    .form-group label { display: block; font-size: 13px; font-weight: 600; color: var(--slate); margin-bottom: 8px; }
    .form-group input, .form-group select, .form-group textarea {
      width: 100%; padding: 12px 16px; border: 1.5px solid var(--gray-200);
      border-radius: 10px; font-size: 14px; font-family: var(--font-body);
      color: var(--slate); background: var(--white);
      transition: border-color .2s, box-shadow .2s;
      outline: none;
    }
    .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
      border-color: var(--blue); box-shadow: 0 0 0 4px rgba(37,99,235,.1);
    }
    .form-group textarea { resize: vertical; min-height: 120px; }
    .form-submit { width: 100%; padding: 14px; background: var(--blue); color: var(--white); border: none; border-radius: 10px; font-size: 15px; font-weight: 600; cursor: pointer; transition: background .2s, transform .2s, box-shadow .2s; font-family: var(--font-body); }
    .form-submit:hover { background: var(--blue-lt); transform: translateY(-1px); box-shadow: 0 6px 20px rgba(37,99,235,.3); }
    .map-placeholder {
      margin-top: 36px; border-radius: 14px; overflow: hidden;
      border: 1px solid var(--gray-200); height: 200px;
      background: linear-gradient(135deg, #e0e7ff 0%, #cffafe 100%);
      display: flex; align-items: center; justify-content: center;
      position: relative;
    }
    .map-pin { font-size: 40px; animation: bounce 2s ease-in-out infinite; }
    @keyframes bounce { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-10px); } }
    .map-label { position: absolute; bottom: 16px; left: 50%; transform: translateX(-50%); background: var(--white); padding: 6px 16px; border-radius: 20px; font-size: 13px; font-weight: 600; color: var(--slate); box-shadow: 0 4px 12px rgba(0,0,0,.1); white-space: nowrap; }

    /* ─── CTA BAND ──────────────────────────────────────────── */
    #cta-band {
      background: linear-gradient(135deg, var(--blue) 0%, #1D4ED8 40%, #0E7490 100%);
      padding: 80px 0; text-align: center; position: relative; overflow: hidden;
    }
    #cta-band::before {
      content: ''; position: absolute; inset: 0;
      background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.04'%3E%3Ccircle cx='30' cy='30' r='2'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    }
    .cta-band-title { font-family: var(--font-display); font-size: clamp(2rem, 4vw, 3rem); font-weight: 800; color: var(--white); margin-bottom: 16px; position: relative; }
    .cta-band-sub { font-size: 1.1rem; color: rgba(255,255,255,.75); margin-bottom: 36px; position: relative; }
    .cta-band-btns { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap; position: relative; }
    .btn-white { background: var(--white); color: var(--blue); }
    .btn-white:hover { background: var(--gray-100); }

    /* ─── FOOTER ────────────────────────────────────────────── */
    #footer { background: var(--slate); padding-top: 80px; }
    .footer-top { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 48px; padding-bottom: 60px; border-bottom: 1px solid rgba(255,255,255,.07); }
    .footer-brand-desc { font-size: 14px; color: rgba(255,255,255,.45); line-height: 1.7; margin-top: 16px; max-width: 300px; }
    .footer-col-title { font-weight: 700; font-size: 14px; color: var(--white); margin-bottom: 20px; }
    .footer-links { list-style: none; display: flex; flex-direction: column; gap: 12px; }
    .footer-links a { font-size: 14px; color: rgba(255,255,255,.45); transition: color .2s; }
    .footer-links a:hover { color: var(--cyan); }
    .footer-bottom { display: flex; align-items: center; justify-content: space-between; padding-block: 28px; flex-wrap: wrap; gap: 12px; }
    .footer-copy { font-size: 13px; color: rgba(255,255,255,.35); }
    .footer-badges { display: flex; gap: 10px; }
    .footer-badge { font-size: 11px; padding: 4px 10px; border-radius: 4px; background: rgba(255,255,255,.06); color: rgba(255,255,255,.4); font-weight: 500; }

    /* ─── Scroll reveal ─────────────────────────────────────── */
    .reveal { opacity: 0; transform: translateY(28px); transition: opacity .6s, transform .6s; }
    .reveal.visible { opacity: 1; transform: none; }

    /* ─── RESPONSIVE ────────────────────────────────────────── */
    @media (max-width: 1024px) {
      .tech-grid { grid-template-columns: repeat(4, 1fr); }
      .portfolio-grid { grid-template-columns: 1fr 1fr; }
      .proj-card:nth-child(n) { grid-column: span 1; }
    }
    @media (max-width: 900px) {
      .about-grid { grid-template-columns: 1fr; gap: 60px; }
      .about-img-wrap { max-width: 480px; }
      .testi-grid { grid-template-columns: 1fr 1fr; }
      .why-grid { grid-template-columns: 1fr 1fr; }
      .footer-top { grid-template-columns: 1fr 1fr; }
      .hero-card { display: none; }
      .process-grid { grid-template-columns: 1fr 1fr; gap: 32px; }
      .process-grid::before { display: none; }
    }
    @media (max-width: 640px) {
      .section { padding-block: 72px; }
      .nav-links, .nav-cta .btn { display: none; }
      .hamburger { display: flex; }
      .services-grid { grid-template-columns: 1fr; }
      .portfolio-grid { grid-template-columns: 1fr; }
      .testi-grid { grid-template-columns: 1fr; }
      .why-grid { grid-template-columns: 1fr; }
      .tech-grid { grid-template-columns: repeat(2, 1fr); }
      .contact-grid { grid-template-columns: 1fr; gap: 40px; }
      .form-row { grid-template-columns: 1fr; }
      .footer-top { grid-template-columns: 1fr; }
      .footer-bottom { flex-direction: column; text-align: center; }
      .process-grid { grid-template-columns: 1fr; gap: 32px; }
      .about-stats { grid-template-columns: 1fr 1fr; }
    }

    /* dark mode */
    @media (prefers-color-scheme: dark) {
      html:not(.force-light) {
        --white: #0F172A;
        --gray-50: #1E293B;
        --gray-100: #1E293B;
        --gray-200: rgba(255,255,255,.08);
        --gray-600: #94A3B8;
        --slate: #F1F5F9;
        --gray-800: #CBD5E1;
      }
      html:not(.force-light) .section-title { color: #F1F5F9; }
      html:not(.force-light) .service-card { background: #1E293B; }
      html:not(.force-light) .testi-card { background: #1E293B; }
      html:not(.force-light) .why-card { background: #1E293B; }
      html:not(.force-light) .contact-form { background: #1E293B; }
      html:not(.force-light) .contact-item-value { color: #F1F5F9; }
      html:not(.force-light) .about-inner-card { background: #1E293B; }
      html:not(.force-light) .about-inner-card-label { color: #94A3B8; }
      html:not(.force-light) .about-stat { background: #0F172A; }
      html:not(.force-light) .form-group input,
      html:not(.force-light) .form-group select,
      html:not(.force-light) .form-group textarea { background: #0F172A; color: #F1F5F9; }
      html:not(.force-light) .footer-bottom { border-color: rgba(255,255,255,.05); }
    }
  </style>
</head>
<body>

<!-- ═══════════════════════════════════════════ NAV -->
<nav id="nav">
  <div class="container nav-inner">
    <a href="#" class="nav-logo">Rubex<span>UI</span></a>
    <ul class="nav-links">
      <li><a href="#about">About</a></li>
      <li><a href="#services">Services</a></li>
      <li><a href="#portfolio">Work</a></li>
      <li><a href="#process">Process</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
    <div class="nav-cta">
      <a href="#contact" class="btn btn-primary" style="padding:10px 20px;font-size:14px;">Get Started</a>
    </div>
    <button class="hamburger" id="hamburger" aria-label="Menu">
      <span></span><span></span><span></span>
    </button>
  </div>
</nav>

<!-- mobile menu -->
<div class="mobile-menu" id="mobileMenu">
  <a href="#about" onclick="closeMobile()">About</a>
  <a href="#services" onclick="closeMobile()">Services</a>
  <a href="#portfolio" onclick="closeMobile()">Work</a>
  <a href="#process" onclick="closeMobile()">Process</a>
  <a href="#contact" onclick="closeMobile()">Contact</a>
  <a href="#contact" class="btn btn-primary" style="text-align:center;margin-top:8px;" onclick="closeMobile()">Get Started</a>
</div>

<!-- ═══════════════════════════════════════════ HERO -->
<section id="hero">
  <div class="hero-bg"></div>
  <div class="hero-grid"></div>
  <div class="orb orb-1"></div>
  <div class="orb orb-2"></div>
  <div class="orb orb-3"></div>

  <div class="container" style="width:100%;padding-top:40px;padding-bottom:60px;">
    <div class="hero-content">
      <div class="hero-pill">
        <span class="hero-pill-dot"></span>
        Available for new projects in 2025
      </div>
      <h1 class="hero-h1">
        Building Digital<br/>
        Experiences That<br/>
        <span class="accent">Drive Growth</span>
      </h1>
      <p class="hero-sub">
        Custom websites, web applications, and digital solutions engineered for performance and scalability. We turn ideas into products that matter.
      </p>
      <div class="hero-btns">
        <a href="#contact" class="btn btn-primary">
          Get a Free Consultation
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
        <a href="#portfolio" class="btn btn-outline">View Our Work</a>
      </div>

      <div class="hero-stats">
        <div>
          <div class="hero-stat-num">150<span>+</span></div>
          <div class="hero-stat-label">Projects Completed</div>
        </div>
        <div>
          <div class="hero-stat-num">98<span>%</span></div>
          <div class="hero-stat-label">Client Satisfaction</div>
        </div>
        <div>
          <div class="hero-stat-num">6<span>+</span></div>
          <div class="hero-stat-label">Years of Excellence</div>
        </div>
        <div>
          <div class="hero-stat-num">40<span>+</span></div>
          <div class="hero-stat-label">Happy Clients</div>
        </div>
      </div>
    </div>
  </div>

  <!-- floating UI cards -->
  <div class="hero-card hero-card-1">
    <div class="card-label">Project delivery</div>
    <div class="card-metric">On Time</div>
    <div class="card-sub">All 2024 projects delivered early</div>
    <div class="card-bar"><div class="card-bar-fill" style="width:92%"></div></div>
  </div>
  <div class="hero-card hero-card-2">
    <div class="card-label">Active clients</div>
    <div class="card-stack">
      <div class="card-avatar">JK</div>
      <div class="card-avatar">MS</div>
      <div class="card-avatar">AL</div>
      <div class="card-avatar">+</div>
      <span class="card-count">+36 more</span>
    </div>
    <div class="card-sub" style="margin-top:8px;">Across 12 countries</div>
  </div>
</section>

<!-- ═══════════════════════════════════════════ ABOUT -->
<section id="about" class="section">
  <div class="container">
    <div class="about-grid">
      <div class="about-img-wrap reveal">
        <div class="about-img-bg">
          <!-- abstract visual -->
          <svg viewBox="0 0 480 360" xmlns="http://www.w3.org/2000/svg" style="position:absolute;inset:0;width:100%;height:100%;opacity:.6">
            <defs>
              <linearGradient id="g1" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stop-color="#fff" stop-opacity=".2"/>
                <stop offset="100%" stop-color="#fff" stop-opacity="0"/>
              </linearGradient>
            </defs>
            <rect x="40" y="40" width="160" height="120" rx="12" fill="url(#g1)" stroke="rgba(255,255,255,.2)" stroke-width="1"/>
            <rect x="220" y="60" width="220" height="80" rx="10" fill="url(#g1)" stroke="rgba(255,255,255,.2)" stroke-width="1"/>
            <rect x="40" y="190" width="120" height="130" rx="12" fill="url(#g1)" stroke="rgba(255,255,255,.2)" stroke-width="1"/>
            <rect x="180" y="170" width="260" height="80" rx="10" fill="url(#g1)" stroke="rgba(255,255,255,.2)" stroke-width="1"/>
            <rect x="180" y="270" width="120" height="60" rx="10" fill="url(#g1)" stroke="rgba(255,255,255,.15)" stroke-width="1"/>
            <circle cx="320" cy="290" r="30" fill="rgba(255,255,255,.1)" stroke="rgba(255,255,255,.25)" stroke-width="1"/>
            <line x1="200" y1="100" x2="200" y2="170" stroke="rgba(255,255,255,.15)" stroke-width="1.5" stroke-dasharray="4 4"/>
            <line x1="120" y1="160" x2="120" y2="190" stroke="rgba(255,255,255,.15)" stroke-width="1.5" stroke-dasharray="4 4"/>
          </svg>
        </div>
        <div class="about-inner-card">
          <div class="about-inner-card-num">6+</div>
          <div class="about-inner-card-label">Years building<br/>digital products</div>
        </div>
        <div class="about-badge-wrap">
          <span class="about-badge-icon">⚡</span>
          <span class="about-badge-text">Fast Delivery</span>
        </div>
      </div>

      <div class="reveal">
        <span class="eyebrow">About Us</span>
        <h2 class="section-title">We're a team that <em style="font-style:normal;color:var(--blue)">ships</em> products people love</h2>
        <p class="section-sub">
          Rubex UI is a forward-thinking web development agency specializing in high-performance digital solutions. We combine deep technical expertise with a sharp eye for design to build products that don't just look great — they convert and scale.
        </p>
        <p class="section-sub" style="margin-top:16px;">
          From early-stage startups to established enterprises, we partner with organizations who want more than a vendor — they want a team that cares about their outcomes.
        </p>

        <div class="about-stats">
          <div class="about-stat">
            <div class="about-stat-num">150+</div>
            <div class="about-stat-label">Projects Done</div>
          </div>
          <div class="about-stat">
            <div class="about-stat-num">40+</div>
            <div class="about-stat-label">Happy Clients</div>
          </div>
          <div class="about-stat">
            <div class="about-stat-num">6+</div>
            <div class="about-stat-label">Years Active</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════ SERVICES -->
<section id="services" class="section section--alt">
  <div class="container">
    <div class="services-header reveal">
      <span class="eyebrow">What We Do</span>
      <h2 class="section-title">End-to-end digital services</h2>
      <p class="section-sub">From concept to launch and beyond — we cover every layer of the modern web stack so your project never gets stuck.</p>
    </div>
    <div class="services-grid">
      <div class="service-card reveal">
        <div class="service-icon">🌐</div>
        <div class="service-title">Custom Website Development</div>
        <div class="service-desc">Pixel-perfect websites built from scratch. Fast, accessible, and tailored to your brand and conversion goals.</div>
        <div class="service-tags"><span class="service-tag">Next.js</span><span class="service-tag">React</span><span class="service-tag">Tailwind</span></div>
      </div>
      <div class="service-card reveal">
        <div class="service-icon">⚙️</div>
        <div class="service-title">Web Application Development</div>
        <div class="service-desc">Complex, data-driven web apps with clean architecture, real-time features, and rock-solid reliability.</div>
        <div class="service-tags"><span class="service-tag">TypeScript</span><span class="service-tag">Node.js</span><span class="service-tag">PostgreSQL</span></div>
      </div>
      <div class="service-card reveal">
        <div class="service-icon">🎨</div>
        <div class="service-title">UI/UX Design</div>
        <div class="service-desc">Research-informed design systems and prototypes that make the user's journey intuitive from the first click.</div>
        <div class="service-tags"><span class="service-tag">Figma</span><span class="service-tag">Prototyping</span></div>
      </div>
      <div class="service-card reveal">
        <div class="service-icon">🛒</div>
        <div class="service-title">E-commerce Solutions</div>
        <div class="service-desc">High-converting storefronts with custom checkout flows, inventory management, and payment integrations.</div>
        <div class="service-tags"><span class="service-tag">Shopify</span><span class="service-tag">Stripe</span><span class="service-tag">WooCommerce</span></div>
      </div>
      <div class="service-card reveal">
        <div class="service-icon">📝</div>
        <div class="service-title">CMS Development</div>
        <div class="service-desc">Headless CMS setups your team can actually use — no developer needed for everyday content updates.</div>
        <div class="service-tags"><span class="service-tag">Sanity</span><span class="service-tag">Contentful</span><span class="service-tag">WordPress</span></div>
      </div>
      <div class="service-card reveal">
        <div class="service-icon">🔍</div>
        <div class="service-title">SEO Optimization</div>
        <div class="service-desc">Technical SEO baked in from day one: Core Web Vitals, structured data, semantic markup, and speed.</div>
        <div class="service-tags"><span class="service-tag">Core Web Vitals</span><span class="service-tag">Schema</span></div>
      </div>
      <div class="service-card reveal">
        <div class="service-icon">🛠️</div>
        <div class="service-title">Website Maintenance</div>
        <div class="service-desc">Ongoing monitoring, security updates, and iterative improvements so your site keeps performing at its best.</div>
        <div class="service-tags"><span class="service-tag">Monitoring</span><span class="service-tag">Security</span></div>
      </div>
      <div class="service-card reveal">
        <div class="service-icon">🔌</div>
        <div class="service-title">API Integrations</div>
        <div class="service-desc">Connect your product to any third-party service — payments, CRMs, analytics, AI tools, and more.</div>
        <div class="service-tags"><span class="service-tag">REST</span><span class="service-tag">GraphQL</span><span class="service-tag">Webhooks</span></div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════ PORTFOLIO -->
<section id="portfolio" class="section">
  <div class="container">
    <div class="portfolio-header">
      <div class="reveal">
        <span class="eyebrow" style="color:var(--cyan)">Featured Work</span>
        <h2 class="section-title" style="color:var(--white);margin-bottom:0;">Projects we're proud of</h2>
      </div>
      <a href="#contact" class="btn btn-outline reveal" style="flex-shrink:0;">Start Your Project →</a>
    </div>

    <div class="portfolio-grid">
      <!-- Project 1 -->
      <div class="proj-card reveal">
        <div class="proj-mock pm1">
          <div class="pm-nav">
            <span class="pm-dot" style="background:#FF5F57"></span>
            <span class="pm-dot" style="background:#FFBD2E;margin-left:4px"></span>
            <span class="pm-dot" style="background:#28C840;margin-left:4px"></span>
            <span class="pm-logo">FinTrack Pro</span>
            <div class="pm-links"><div class="pm-link"></div><div class="pm-link"></div><div class="pm-link"></div></div>
          </div>
          <div class="pm-hero-text">
            <div class="pm-headline" style="width:80%"></div>
            <div class="pm-headline" style="width:60%"></div>
            <div class="pm-subline"></div>
            <div class="pm-btn" style="background:rgba(37,99,235,.5)"></div>
          </div>
          <div class="pm-content">
            <div class="pm-row" style="gap:12px;flex:1">
              <div class="pm-block" style="flex:1;padding:12px;">
                <div class="pm-bar" style="margin-bottom:6px"><div class="pm-bar-fill" style="width:75%;background:linear-gradient(90deg,#2563EB,#06B6D4)"></div></div>
                <div class="pm-bar" style="margin-bottom:6px"><div class="pm-bar-fill" style="width:50%;background:rgba(37,99,235,.4)"></div></div>
                <div class="pm-bar"><div class="pm-bar-fill" style="width:88%;background:linear-gradient(90deg,#06B6D4,#7C3AED)"></div></div>
              </div>
              <div class="pm-block" style="flex:1;padding:12px;display:flex;align-items:center;justify-content:center;">
                <svg viewBox="0 0 80 60" width="80"><polyline points="0,50 20,35 40,40 60,15 80,20" fill="none" stroke="rgba(6,182,212,.6)" stroke-width="2"/></svg>
              </div>
            </div>
          </div>
        </div>
        <div class="proj-overlay-always">
          <div class="proj-category">SaaS Dashboard</div>
          <div class="proj-name">FinTrack Pro</div>
          <div class="proj-tech"><span>Next.js</span><span>TypeScript</span><span>PostgreSQL</span></div>
          <div class="proj-result">↑ 240% increase in user retention</div>
        </div>
      </div>

      <!-- Project 2 -->
      <div class="proj-card reveal">
        <div class="proj-mock pm2">
          <div class="pm-nav">
            <span class="pm-dot" style="background:#FF5F57"></span>
            <span class="pm-dot" style="background:#FFBD2E;margin-left:4px"></span>
            <span class="pm-dot" style="background:#28C840;margin-left:4px"></span>
            <span class="pm-logo">Velta Shop</span>
          </div>
          <div class="pm-content" style="gap:10px;">
            <div class="pm-row">
              <div class="pm-block" style="flex:1;height:70px;background:rgba(255,255,255,.08)"></div>
              <div class="pm-block" style="flex:1;height:70px;background:rgba(255,255,255,.08)"></div>
            </div>
            <div class="pm-row">
              <div class="pm-block" style="flex:2;height:50px;background:rgba(255,255,255,.06)"></div>
              <div class="pm-block" style="flex:1;height:50px;background:rgba(37,99,235,.3)"></div>
            </div>
            <div class="pm-bar" style="margin-top:4px"><div class="pm-bar-fill" style="width:65%;background:rgba(6,182,212,.5)"></div></div>
          </div>
        </div>
        <div class="proj-overlay-always">
          <div class="proj-category">E-commerce</div>
          <div class="proj-name">Velta Shop</div>
          <div class="proj-tech"><span>React</span><span>Shopify</span><span>Stripe</span></div>
          <div class="proj-result">↑ $2.4M in first-year revenue</div>
        </div>
      </div>

      <!-- Project 3 -->
      <div class="proj-card reveal">
        <div class="proj-mock pm3">
          <div class="pm-nav">
            <span class="pm-dot" style="background:#FF5F57"></span>
            <span class="pm-dot" style="background:#FFBD2E;margin-left:4px"></span>
            <span class="pm-dot" style="background:#28C840;margin-left:4px"></span>
            <span class="pm-logo">HealthSync</span>
          </div>
          <div class="pm-content">
            <div style="display:flex;gap:8px;flex:1">
              <div style="display:flex;flex-direction:column;gap:8px;flex:1">
                <div class="pm-block" style="height:40px;background:rgba(124,58,237,.3)"></div>
                <div class="pm-block" style="flex:1;background:rgba(255,255,255,.05)"></div>
              </div>
              <div class="pm-block" style="flex:2;background:rgba(255,255,255,.04);display:flex;align-items:center;justify-content:center;">
                <svg viewBox="0 0 100 80" width="100"><circle cx="50" cy="40" r="30" fill="none" stroke="rgba(124,58,237,.5)" stroke-width="2"/><circle cx="50" cy="40" r="20" fill="none" stroke="rgba(6,182,212,.4)" stroke-width="2"/><circle cx="50" cy="40" r="10" fill="rgba(124,58,237,.3)"/></svg>
              </div>
            </div>
          </div>
        </div>
        <div class="proj-overlay-always">
          <div class="proj-category">Healthcare App</div>
          <div class="proj-name">HealthSync</div>
          <div class="proj-tech"><span>React</span><span>Node.js</span><span>AWS</span></div>
          <div class="proj-result">↑ 50K active patients onboarded</div>
        </div>
      </div>

      <!-- Project 4 -->
      <div class="proj-card reveal">
        <div class="proj-mock pm4">
          <div class="pm-nav">
            <span class="pm-dot" style="background:#FF5F57"></span>
            <span class="pm-dot" style="background:#FFBD2E;margin-left:4px"></span>
            <span class="pm-dot" style="background:#28C840;margin-left:4px"></span>
            <span class="pm-logo">BuildFlow</span>
            <div class="pm-links" style="margin-left:auto"><div class="pm-link"></div><div class="pm-link"></div></div>
          </div>
          <div class="pm-hero-text" style="padding:16px">
            <div class="pm-headline" style="width:70%"></div>
            <div class="pm-subline" style="margin-top:6px"></div>
            <div class="pm-subline" style="margin-top:6px;width:50%"></div>
          </div>
          <div class="pm-content" style="flex-direction:row;gap:12px">
            <div class="pm-block" style="flex:1;height:60px;background:rgba(1,73,124,.6)"></div>
            <div class="pm-block" style="flex:1;height:60px;background:rgba(6,182,212,.15)"></div>
            <div class="pm-block" style="flex:1;height:60px;background:rgba(37,99,235,.2)"></div>
          </div>
        </div>
        <div class="proj-overlay-always">
          <div class="proj-category">Construction Tech</div>
          <div class="proj-name">BuildFlow Platform</div>
          <div class="proj-tech"><span>Next.js</span><span>GraphQL</span><span>Docker</span></div>
          <div class="proj-result">↑ 60% reduction in project delays</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════ PROCESS -->
<section id="process" class="section section--alt">
  <div class="container">
    <div style="text-align:center;max-width:560px;margin-inline:auto;" class="reveal">
      <span class="eyebrow">How We Work</span>
      <h2 class="section-title">A process built for predictability</h2>
      <p class="section-sub" style="margin-inline:auto;">No surprises. Every project runs through the same battle-tested workflow so you always know where things stand.</p>
    </div>
    <div class="process-grid">
      <div class="process-step reveal">
        <div class="process-num">
          <div class="process-num-inner">01</div>
        </div>
        <span class="process-icon">🔭</span>
        <div class="process-step-title">Discovery & Strategy</div>
        <div class="process-step-desc">We dig into your goals, users, and competitive landscape to build a clear product strategy before writing a line of code.</div>
      </div>
      <div class="process-step reveal">
        <div class="process-num">
          <div class="process-num-inner">02</div>
        </div>
        <span class="process-icon">✏️</span>
        <div class="process-step-title">Design & Prototyping</div>
        <div class="process-step-desc">Interactive Figma prototypes that let you see and feel the product early — before any build commitment is made.</div>
      </div>
      <div class="process-step reveal">
        <div class="process-num">
          <div class="process-num-inner">03</div>
        </div>
        <span class="process-icon">💻</span>
        <div class="process-step-title">Development & Testing</div>
        <div class="process-step-desc">Agile sprints with weekly demos. Every feature is code-reviewed and QA'd across devices before it ships.</div>
      </div>
      <div class="process-step reveal">
        <div class="process-num">
          <div class="process-num-inner">04</div>
        </div>
        <span class="process-icon">🚀</span>
        <div class="process-step-title">Launch & Growth</div>
        <div class="process-step-desc">Smooth deployment with zero-downtime releases, performance monitoring, and post-launch iteration cycles.</div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════ TESTIMONIALS -->
<section id="testimonials" class="section">
  <div class="container">
    <div style="text-align:center;max-width:560px;margin-inline:auto;" class="reveal">
      <span class="eyebrow">Client Stories</span>
      <h2 class="section-title">Words from people we've worked with</h2>
    </div>
    <div class="testi-grid">
      <div class="testi-card reveal">
        <div class="testi-stars">★★★★★</div>
        <p class="testi-text">"Rubex UI completely transformed our online presence. Our new platform is faster, more polished, and our conversion rate jumped 80% in the first month."</p>
        <div class="testi-author">
          <div class="testi-avatar" style="background:#7C3AED;">JK</div>
          <div>
            <div class="testi-name">James Kowalski</div>
            <div class="testi-role">CEO, FinTrack Pro</div>
          </div>
        </div>
      </div>
      <div class="testi-card reveal">
        <div class="testi-stars">★★★★★</div>
        <p class="testi-text">"Working with Rubex UI felt like having a senior engineering team embedded in our company. They anticipated problems before they happened and delivered ahead of schedule."</p>
        <div class="testi-author">
          <div class="testi-avatar" style="background:#2563EB;">MS</div>
          <div>
            <div class="testi-name">Maya Singh</div>
            <div class="testi-role">CTO, HealthSync</div>
          </div>
        </div>
      </div>
      <div class="testi-card reveal">
        <div class="testi-stars">★★★★★</div>
        <p class="testi-text">"They took our outdated e-commerce site and turned it into a revenue engine. The new Velta storefront paid for the entire project in its first week live."</p>
        <div class="testi-author">
          <div class="testi-avatar" style="background:#10B981;">AL</div>
          <div>
            <div class="testi-name">Alex Larkin</div>
            <div class="testi-role">Founder, Velta Shop</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════ WHY CHOOSE -->
<section id="why" class="section section--alt">
  <div class="container">
    <div style="text-align:center;max-width:560px;margin-inline:auto;" class="reveal">
      <span class="eyebrow">Why Rubex UI</span>
      <h2 class="section-title">Built differently, on purpose</h2>
      <p class="section-sub" style="margin-inline:auto;">We made deliberate choices about how we operate so your experience as a client is just as good as the product we build you.</p>
    </div>
    <div class="why-grid">
      <div class="why-card reveal">
        <div class="why-icon">⚡</div>
        <div>
          <div class="why-title">Fast Delivery</div>
          <div class="why-desc">We scope tightly and ship in focused sprints. Most projects launch 10–20% ahead of the agreed timeline.</div>
        </div>
      </div>
      <div class="why-card reveal">
        <div class="why-icon">🛠️</div>
        <div>
          <div class="why-title">Modern Technologies</div>
          <div class="why-desc">We build on the current best-in-class stack — not frameworks we're comfortable with but frameworks that age well.</div>
        </div>
      </div>
      <div class="why-card reveal">
        <div class="why-icon">📱</div>
        <div>
          <div class="why-title">Responsive Design</div>
          <div class="why-desc">Every interface is designed mobile-first and tested on real devices before it ever ships to production.</div>
        </div>
      </div>
      <div class="why-card reveal">
        <div class="why-icon">🔍</div>
        <div>
          <div class="why-title">SEO-Friendly Development</div>
          <div class="why-desc">Performance, structured data, and semantic HTML are non-negotiables — not add-ons you have to request.</div>
        </div>
      </div>
      <div class="why-card reveal">
        <div class="why-icon">🤝</div>
        <div>
          <div class="why-title">Dedicated Support</div>
          <div class="why-desc">A direct line to your lead developer — not a ticket queue. Real answers from the people who built your product.</div>
        </div>
      </div>
      <div class="why-card reveal">
        <div class="why-icon">📈</div>
        <div>
          <div class="why-title">Scalable Solutions</div>
          <div class="why-desc">Architecture designed to grow with you. What works for 100 users works for 100,000, without a full rewrite.</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════ TECHNOLOGIES -->
<section id="technologies" class="section">
  <div class="container">
    <div class="tech-header reveal">
      <span class="eyebrow" style="color:var(--cyan)">Tech Stack</span>
      <h2 class="section-title">Tools we trust</h2>
      <p class="section-sub">We stay current with the technologies that power the best products on the web — and go deep on them.</p>
    </div>
    <div class="tech-grid">
      <div class="tech-card reveal">
        <span class="tech-logo">⚛️</span>
        <div class="tech-name">React</div>
        <div class="tech-desc">UI library</div>
      </div>
      <div class="tech-card reveal">
        <span class="tech-logo">▲</span>
        <div class="tech-name">Next.js</div>
        <div class="tech-desc">Full-stack framework</div>
      </div>
      <div class="tech-card reveal">
        <span class="tech-logo">📘</span>
        <div class="tech-name">TypeScript</div>
        <div class="tech-desc">Type-safe JavaScript</div>
      </div>
      <div class="tech-card reveal">
        <span class="tech-logo">🟢</span>
        <div class="tech-name">Node.js</div>
        <div class="tech-desc">Backend runtime</div>
      </div>
      <div class="tech-card reveal">
        <span class="tech-logo">🎨</span>
        <div class="tech-name">Tailwind CSS</div>
        <div class="tech-desc">Utility-first CSS</div>
      </div>
      <div class="tech-card reveal">
        <span class="tech-logo">🐘</span>
        <div class="tech-name">PostgreSQL</div>
        <div class="tech-desc">Relational database</div>
      </div>
      <div class="tech-card reveal">
        <span class="tech-logo">☁️</span>
        <div class="tech-name">AWS</div>
        <div class="tech-desc">Cloud infrastructure</div>
      </div>
      <div class="tech-card reveal">
        <span class="tech-logo">🐋</span>
        <div class="tech-name">Docker</div>
        <div class="tech-desc">Containerization</div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════ CONTACT -->
<section id="contact" class="section section--alt">
  <div class="container">
    <div style="text-align:center;max-width:560px;margin-inline:auto;margin-bottom:56px;" class="reveal">
      <span class="eyebrow">Get in Touch</span>
      <h2 class="section-title">Let's build something together</h2>
      <p class="section-sub" style="margin-inline:auto;">Tell us about your project. We'll get back to you within one business day with an honest assessment and next steps.</p>
    </div>
    <div class="contact-grid">
      <div class="reveal">
        <div class="contact-item">
          <div class="contact-item-icon">📧</div>
          <div>
            <div class="contact-item-label">Email</div>
            <div class="contact-item-value">hello@rubexui.com</div>
          </div>
        </div>
        <div class="contact-item">
          <div class="contact-item-icon">📞</div>
          <div>
            <div class="contact-item-label">Phone</div>
            <div class="contact-item-value">+1 (555) 234-5678</div>
          </div>
        </div>
        <div class="contact-item">
          <div class="contact-item-icon">📍</div>
          <div>
            <div class="contact-item-label">Location</div>
            <div class="contact-item-value">San Francisco, CA · Remote-first</div>
          </div>
        </div>
        <div class="contact-item">
          <div class="contact-item-icon">⏰</div>
          <div>
            <div class="contact-item-label">Response Time</div>
            <div class="contact-item-value">Within 1 business day</div>
          </div>
        </div>
        <div class="social-links">
          <div class="social-link" title="Twitter/X">𝕏</div>
          <div class="social-link" title="LinkedIn">in</div>
          <div class="social-link" title="GitHub">⌥</div>
          <div class="social-link" title="Dribbble">🏀</div>
        </div>
        <div class="map-placeholder">
          <div class="map-pin">📍</div>
          <div class="map-label">San Francisco Bay Area</div>
        </div>
      </div>

      <div class="contact-form reveal">
        <h3 style="font-family:var(--font-display);font-size:1.35rem;font-weight:800;margin-bottom:24px;color:var(--slate)">Tell us about your project</h3>
        <div class="form-row">
          <div class="form-group">
            <label for="fname">First Name</label>
            <input type="text" id="fname" placeholder="Alex" />
          </div>
          <div class="form-group">
            <label for="lname">Last Name</label>
            <input type="text" id="lname" placeholder="Johnson" />
          </div>
        </div>
        <div class="form-group">
          <label for="email">Email Address</label>
          <input type="email" id="email" placeholder="alex@company.com" />
        </div>
        <div class="form-group">
          <label for="service">Service Needed</label>
          <select id="service">
            <option value="">Select a service...</option>
            <option>Custom Website Development</option>
            <option>Web Application Development</option>
            <option>UI/UX Design</option>
            <option>E-commerce Solutions</option>
            <option>CMS Development</option>
            <option>SEO Optimization</option>
            <option>Website Maintenance</option>
            <option>API Integrations</option>
          </select>
        </div>
        <div class="form-group">
          <label for="message">Project Details</label>
          <textarea id="message" placeholder="Tell us about your project, timeline, and budget..."></textarea>
        </div>
        <button class="form-submit" onclick="handleSubmit(this)">Send Message →</button>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════ CTA BAND -->
<section id="cta-band">
  <div class="container">
    <div class="cta-band-title">Ready to start your project?</div>
    <p class="cta-band-sub">Free consultation. No commitment. Just a conversation about what you're building.</p>
    <div class="cta-band-btns">
      <a href="#contact" class="btn btn-white">Book a Free Call</a>
      <a href="mailto:hello@rubexui.com" class="btn btn-outline">Email Us Directly</a>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════════════════ FOOTER -->
<footer id="footer">
  <div class="container">
    <div class="footer-top">
      <div>
        <div class="nav-logo" style="font-size:24px;">Rubex<span style="color:var(--cyan)">UI</span></div>
        <p class="footer-brand-desc">We build web experiences that drive real business results. From landing pages to full-scale applications — crafted with care.</p>
        <div class="social-links" style="margin-top:24px;">
          <div class="social-link" style="border-color:rgba(255,255,255,.12);color:rgba(255,255,255,.5)">𝕏</div>
          <div class="social-link" style="border-color:rgba(255,255,255,.12);color:rgba(255,255,255,.5)">in</div>
          <div class="social-link" style="border-color:rgba(255,255,255,.12);color:rgba(255,255,255,.5)">⌥</div>
          <div class="social-link" style="border-color:rgba(255,255,255,.12);color:rgba(255,255,255,.5)">🏀</div>
        </div>
      </div>
      <div>
        <div class="footer-col-title">Company</div>
        <ul class="footer-links">
          <li><a href="#about">About Us</a></li>
          <li><a href="#portfolio">Our Work</a></li>
          <li><a href="#process">How We Work</a></li>
          <li><a href="#contact">Contact</a></li>
          <li><a href="#">Careers</a></li>
        </ul>
      </div>
      <div>
        <div class="footer-col-title">Services</div>
        <ul class="footer-links">
          <li><a href="#services">Website Development</a></li>
          <li><a href="#services">Web Applications</a></li>
          <li><a href="#services">UI/UX Design</a></li>
          <li><a href="#services">E-commerce</a></li>
          <li><a href="#services">SEO</a></li>
        </ul>
      </div>
      <div>
        <div class="footer-col-title">Legal</div>
        <ul class="footer-links">
          <li><a href="#">Privacy Policy</a></li>
          <li><a href="#">Terms of Service</a></li>
          <li><a href="#">Cookie Policy</a></li>
        </ul>
        <div style="margin-top:24px;">
          <div class="footer-col-title">Get in touch</div>
          <div style="font-size:14px;color:rgba(255,255,255,.45)">hello@rubexui.com</div>
          <div style="font-size:14px;color:rgba(255,255,255,.45);margin-top:6px;">+1 (555) 234-5678</div>
        </div>
      </div>
    </div>
    <div class="footer-bottom">
      <div class="footer-copy">© 2025 Rubex UI. All rights reserved.</div>
      <div class="footer-badges">
        <span class="footer-badge">Next.js 15</span>
        <span class="footer-badge">TypeScript</span>
        <span class="footer-badge">WCAG 2.1</span>
      </div>
    </div>
  </div>
</footer>

<script>
  /* ─── NAV scroll effect ─── */
  const nav = document.getElementById('nav');
  window.addEventListener('scroll', () => {
    nav.classList.toggle('scrolled', window.scrollY > 40);
  }, { passive: true });

  /* ─── Mobile menu ─── */
  const hamburger = document.getElementById('hamburger');
  const mobileMenu = document.getElementById('mobileMenu');
  hamburger.addEventListener('click', () => mobileMenu.classList.toggle('open'));
  function closeMobile() { mobileMenu.classList.remove('open'); }

  /* ─── Scroll reveal ─── */
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((e, i) => {
      if (e.isIntersecting) {
        setTimeout(() => e.target.classList.add('visible'), i * 60);
        observer.unobserve(e.target);
      }
    });
  }, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });

  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

  /* ─── Counter animation ─── */
  function animateCounter(el, target, suffix) {
    let start = 0;
    const duration = 1600;
    const step = (timestamp) => {
      if (!start) start = timestamp;
      const progress = Math.min((timestamp - start) / duration, 1);
      const eased = 1 - Math.pow(1 - progress, 3);
      el.textContent = Math.round(eased * target) + suffix;
      if (progress < 1) requestAnimationFrame(step);
    };
    requestAnimationFrame(step);
  }

  const statsObs = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        const nums = e.target.querySelectorAll('.about-stat-num');
        nums.forEach(n => {
          const text = n.textContent;
          const match = text.match(/(\d+)(\+|%)?/);
          if (match) animateCounter(n, parseInt(match[1]), match[2] || '');
        });
        statsObs.unobserve(e.target);
      }
    });
  }, { threshold: 0.5 });

  const statsSection = document.querySelector('.about-stats');
  if (statsSection) statsObs.observe(statsSection);

  /* ─── Form handler ─── */
  function handleSubmit(btn) {
    const orig = btn.textContent;
    btn.textContent = 'Sending…';
    btn.disabled = true;
    setTimeout(() => {
      btn.textContent = '✓ Message Sent!';
      btn.style.background = '#10B981';
      setTimeout(() => {
        btn.textContent = orig;
        btn.style.background = '';
        btn.disabled = false;
      }, 3000);
    }, 1200);
  }

  /* ─── Smooth scroll nav links ─── */
  document.querySelectorAll('a[href^="#"]').forEach(link => {
    link.addEventListener('click', e => {
      const id = link.getAttribute('href').slice(1);
      const el = document.getElementById(id);
      if (el) {
        e.preventDefault();
        const top = el.getBoundingClientRect().top + window.scrollY - 72;
        window.scrollTo({ top, behavior: 'smooth' });
      }
    });
  });
</script>
</body>
</html>
